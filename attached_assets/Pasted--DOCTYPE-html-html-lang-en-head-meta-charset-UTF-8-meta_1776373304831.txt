<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>WOLF TECH — Build. Deploy. Dominate.</title>
  <meta name="description" content="WOLF TECH — The developer portfolio of Silent Wolf. Full-stack developer, bot engineer, and infrastructure builder." />
  <meta property="og:title" content="WOLF TECH — Build. Deploy. Dominate." />
  <meta property="og:description" content="Full-stack developer, bot engineer, and infrastructure builder." />
  <meta property="og:type" content="website" />

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet" />

  <style>
    /* ─── RESET & BASE ─── */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      background: #000;
      color: #00ff00;
      font-family: 'JetBrains Mono', monospace;
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }
    a { text-decoration: none; color: inherit; }

    /* ─── CSS VARIABLES ─── */
    :root {
      --primary: #00ff00;
      --primary-dim: rgba(0,255,0,0.15);
      --primary-border: rgba(0,255,0,0.2);
      --primary-border-hover: rgba(0,255,0,0.4);
      --primary-glow: 0 0 20px rgba(0,255,0,0.3);
      --primary-glow-lg: 0 0 40px rgba(0,255,0,0.2);
      --gray-800: #1a1a1a;
      --gray-700: #2a2a2a;
      --gray-500: #6b7280;
      --gray-400: #9ca3af;
      --gray-300: #d1d5db;
      --white: #ffffff;
      --card-bg: rgba(0,0,0,0.4);
      --glass-bg: rgba(0,255,0,0.03);
    }

    /* ─── NEON GRID BACKGROUND ─── */
    .neon-bg {
      position: fixed;
      inset: 0;
      z-index: 0;
      background:
        linear-gradient(rgba(0,255,0,0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,255,0,0.03) 1px, transparent 1px);
      background-size: 50px 50px;
      pointer-events: none;
    }
    .neon-bg::after {
      content: '';
      position: absolute;
      inset: 0;
      background: radial-gradient(ellipse 80% 60% at 50% 0%, rgba(0,255,0,0.06) 0%, transparent 70%);
    }

    /* ─── SCROLLBAR ─── */
    ::-webkit-scrollbar { width: 4px; }
    ::-webkit-scrollbar-track { background: #000; }
    ::-webkit-scrollbar-thumb { background: rgba(0,255,0,0.3); border-radius: 2px; }

    /* ─── LAYOUT ─── */
    .container { max-width: 1280px; margin: 0 auto; padding: 0 1.5rem; }
    .relative { position: relative; }
    .z10 { position: relative; z-index: 10; }

    /* ─── NAV ─── */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 50;
      background: rgba(0,0,0,0.95);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--primary-border);
    }
    .nav-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      height: 64px;
    }
    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .logo-icon {
      width: 40px; height: 40px;
      border-radius: 10px;
      background: rgba(0,255,0,0.05);
      border: 1px solid var(--primary-border);
      display: flex; align-items: center; justify-content: center;
      animation: glowPulse 3s ease-in-out infinite;
    }
    .logo-icon svg { width: 22px; height: 22px; }
    .logo-text { font-family: 'Orbitron', monospace; font-weight: 900; font-size: 1.2rem; letter-spacing: 0.15em; }
    .logo-wolf { color: var(--primary); }
    .logo-tech { color: var(--gray-300); }
    .logo-sub { font-size: 0.65rem; color: var(--gray-500); font-family: 'JetBrains Mono', monospace; margin-top: 2px; }

    .nav-links { display: flex; align-items: center; gap: 8px; }
    .nav-link {
      padding: 6px 14px;
      font-size: 0.8rem;
      color: var(--gray-400);
      border: 1px solid rgba(0,255,0,0.15);
      border-radius: 8px;
      transition: all 0.2s;
      cursor: pointer;
      background: transparent;
      font-family: 'JetBrains Mono', monospace;
    }
    .nav-link:hover { color: var(--white); background: rgba(0,255,0,0.05); border-color: var(--primary-border-hover); }
    .nav-cta {
      padding: 6px 14px;
      font-size: 0.8rem;
      color: var(--primary);
      border: 1px solid rgba(0,255,0,0.3);
      border-radius: 8px;
      background: rgba(0,255,0,0.08);
      transition: all 0.2s;
      cursor: pointer;
      font-family: 'JetBrains Mono', monospace;
    }
    .nav-cta:hover { background: rgba(0,255,0,0.15); }

    /* ─── HERO ─── */
    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 8rem 1.5rem 4rem;
      position: relative;
      z-index: 10;
    }
    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 6px 16px;
      border: 1px solid rgba(0,255,0,0.3);
      border-radius: 999px;
      background: rgba(0,255,0,0.05);
      font-size: 0.75rem;
      color: var(--primary);
      margin-bottom: 2rem;
      animation: fadeInDown 0.8s ease both;
    }
    .hero-badge-dot {
      width: 6px; height: 6px;
      border-radius: 50%;
      background: var(--primary);
      animation: blink 1.5s ease-in-out infinite;
    }
    .hero-title {
      font-family: 'Orbitron', monospace;
      font-weight: 900;
      font-size: clamp(2.5rem, 8vw, 5.5rem);
      line-height: 1.05;
      letter-spacing: -0.02em;
      margin-bottom: 1.5rem;
      animation: fadeInUp 0.8s ease 0.1s both;
    }
    .hero-title-white { color: var(--white); }
    .hero-title-green {
      background: linear-gradient(135deg, rgba(0,255,0,0.9), rgba(0,255,0,0.6));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .hero-tagline {
      font-size: 1rem;
      color: var(--primary);
      font-family: 'JetBrains Mono', monospace;
      margin-bottom: 0.5rem;
      animation: fadeInUp 0.8s ease 0.2s both;
    }
    .hero-desc {
      max-width: 560px;
      font-size: 1rem;
      color: var(--gray-400);
      line-height: 1.7;
      margin: 0.75rem auto 2.5rem;
      animation: fadeInUp 0.8s ease 0.3s both;
    }
    .hero-btns {
      display: flex;
      gap: 12px;
      justify-content: center;
      flex-wrap: wrap;
      animation: fadeInUp 0.8s ease 0.5s both;
    }
    .btn-primary {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 28px;
      background: rgba(0,255,0,0.1);
      color: var(--primary);
      border: 1px solid rgba(0,255,0,0.3);
      border-radius: 10px;
      font-family: 'Orbitron', monospace;
      font-weight: 700;
      font-size: 0.85rem;
      letter-spacing: 0.05em;
      cursor: pointer;
      transition: all 0.2s;
    }
    .btn-primary:hover { background: rgba(0,255,0,0.18); transform: translateY(-2px); box-shadow: var(--primary-glow); }
    .btn-ghost {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 28px;
      background: transparent;
      color: var(--gray-300);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 10px;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.85rem;
      cursor: pointer;
      transition: all 0.2s;
    }
    .btn-ghost:hover { color: var(--white); background: rgba(255,255,255,0.05); border-color: rgba(255,255,255,0.2); }

    /* ─── STATS ─── */
    .stats-row {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
      max-width: 700px;
      margin: 3rem auto 0;
      animation: fadeInUp 0.8s ease 0.7s both;
    }
    .stat-card {
      padding: 16px;
      border-radius: 12px;
      border: 1px solid var(--primary-border);
      background: rgba(0,0,0,0.3);
      backdrop-filter: blur(8px);
      text-align: center;
      transition: border-color 0.2s;
    }
    .stat-card:hover { border-color: var(--primary-border-hover); }
    .stat-value { font-family: 'Orbitron', monospace; font-size: 1.6rem; font-weight: 900; color: var(--white); }
    .stat-label { font-size: 0.7rem; color: var(--gray-500); margin-top: 4px; }

    /* ─── SECTIONS ─── */
    section { position: relative; z-index: 10; padding: 5rem 1.5rem; }
    .section-label {
      font-size: 0.7rem;
      letter-spacing: 0.2em;
      color: var(--primary);
      text-transform: uppercase;
      margin-bottom: 0.75rem;
      font-family: 'JetBrains Mono', monospace;
    }
    .section-title {
      font-family: 'Orbitron', monospace;
      font-weight: 900;
      font-size: clamp(1.6rem, 4vw, 2.5rem);
      color: var(--white);
      margin-bottom: 1rem;
    }
    .section-desc { color: var(--gray-400); max-width: 560px; line-height: 1.7; font-size: 0.95rem; }

    /* ─── ABOUT ─── */
    .about-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 48px;
      align-items: center;
      max-width: 1100px;
      margin: 0 auto;
    }
    .about-avatar-wrap {
      display: flex;
      justify-content: center;
    }
    .about-avatar {
      width: 260px; height: 260px;
      border-radius: 20px;
      border: 2px solid var(--primary-border);
      background: linear-gradient(135deg, rgba(0,255,0,0.05), rgba(0,0,0,0.8));
      display: flex; align-items: center; justify-content: center;
      animation: glowPulse 4s ease-in-out infinite;
      position: relative;
      overflow: hidden;
    }
    .about-avatar::before {
      content: '';
      position: absolute;
      inset: 0;
      background: repeating-linear-gradient(
        45deg,
        rgba(0,255,0,0.03) 0px,
        rgba(0,255,0,0.03) 1px,
        transparent 1px,
        transparent 14px
      );
    }
    .about-avatar svg { width: 80px; height: 80px; opacity: 0.6; }
    .about-content { }
    .about-list { list-style: none; margin-top: 1.5rem; display: flex; flex-direction: column; gap: 12px; }
    .about-list li {
      display: flex; align-items: center; gap: 10px;
      font-size: 0.9rem; color: var(--gray-400);
    }
    .about-list li::before { content: '▸'; color: var(--primary); font-size: 0.8rem; }

    /* ─── PROJECTS ─── */
    .projects-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
      max-width: 1100px;
      margin: 3rem auto 0;
    }
    .project-card {
      padding: 24px;
      border-radius: 16px;
      border: 1px solid var(--primary-border);
      background: var(--card-bg);
      backdrop-filter: blur(8px);
      transition: all 0.3s;
      cursor: pointer;
      position: relative;
      overflow: hidden;
    }
    .project-card:hover {
      border-color: var(--primary-border-hover);
      transform: translateY(-5px);
      box-shadow: var(--primary-glow-lg);
    }
    .project-card.featured { border-color: rgba(0,255,0,0.4); }
    .project-card.featured::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 2px;
      background: linear-gradient(90deg, rgba(0,255,0,0.3), var(--primary), rgba(0,255,0,0.3));
    }
    .project-tag {
      position: absolute;
      top: 12px; right: 12px;
      font-size: 0.6rem;
      padding: 3px 10px;
      border-radius: 999px;
      background: rgba(0,255,0,0.1);
      color: var(--primary);
      border: 1px solid rgba(0,255,0,0.25);
      font-family: 'JetBrains Mono', monospace;
      letter-spacing: 0.1em;
    }
    .project-icon {
      width: 48px; height: 48px;
      border-radius: 12px;
      background: rgba(0,255,0,0.06);
      border: 1px solid var(--primary-border);
      display: flex; align-items: center; justify-content: center;
      margin-bottom: 16px;
      transition: transform 0.3s;
    }
    .project-card:hover .project-icon { transform: scale(1.1); }
    .project-icon svg { width: 22px; height: 22px; }
    .project-name {
      font-family: 'Orbitron', monospace;
      font-weight: 700;
      font-size: 1rem;
      color: var(--white);
      margin-bottom: 8px;
    }
    .project-desc { font-size: 0.82rem; color: var(--gray-400); line-height: 1.6; margin-bottom: 16px; }
    .project-tech {
      display: flex; flex-wrap: wrap; gap: 6px;
    }
    .tech-badge {
      font-size: 0.65rem;
      padding: 3px 10px;
      border-radius: 999px;
      border: 1px solid rgba(0,255,0,0.15);
      color: var(--primary);
      font-family: 'JetBrains Mono', monospace;
      background: rgba(0,255,0,0.04);
    }
    .project-link {
      display: inline-flex; align-items: center; gap: 6px;
      font-size: 0.78rem;
      color: rgba(0,255,0,0.7);
      margin-top: 14px;
      transition: color 0.2s;
      opacity: 0;
      transition: all 0.3s;
    }
    .project-card:hover .project-link { opacity: 1; color: var(--primary); }

    /* ─── SKILLS ─── */
    .skills-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 14px;
      max-width: 1000px;
      margin: 3rem auto 0;
    }
    .skill-card {
      padding: 20px 16px;
      border-radius: 12px;
      border: 1px solid var(--primary-border);
      background: var(--card-bg);
      text-align: center;
      transition: all 0.2s;
    }
    .skill-card:hover { border-color: var(--primary-border-hover); background: rgba(0,255,0,0.04); transform: translateY(-3px); }
    .skill-icon { font-size: 1.8rem; margin-bottom: 8px; }
    .skill-name { font-family: 'Orbitron', monospace; font-size: 0.72rem; font-weight: 700; color: var(--white); letter-spacing: 0.05em; }
    .skill-level { font-size: 0.65rem; color: var(--gray-500); margin-top: 4px; }

    /* ─── CTA ─── */
    .cta-box {
      max-width: 720px;
      margin: 0 auto;
      padding: 56px 48px;
      border-radius: 20px;
      border: 1px solid var(--primary-border);
      background: rgba(0,0,0,0.4);
      backdrop-filter: blur(8px);
      text-align: center;
    }
    .cta-title {
      font-family: 'Orbitron', monospace;
      font-weight: 900;
      font-size: clamp(1.4rem, 3vw, 2rem);
      color: var(--white);
      margin-bottom: 1rem;
    }
    .cta-desc { color: var(--gray-400); font-size: 0.95rem; margin-bottom: 2rem; line-height: 1.7; }
    .contact-links { display: flex; gap: 12px; justify-content: center; flex-wrap: wrap; }
    .contact-link {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 10px 22px;
      border-radius: 10px;
      font-size: 0.82rem;
      font-family: 'JetBrains Mono', monospace;
      border: 1px solid var(--primary-border);
      color: var(--primary);
      background: rgba(0,255,0,0.06);
      transition: all 0.2s;
      cursor: pointer;
    }
    .contact-link:hover { background: rgba(0,255,0,0.12); border-color: var(--primary-border-hover); transform: translateY(-2px); }
    .contact-link.white { color: var(--gray-300); background: transparent; border-color: rgba(255,255,255,0.1); }
    .contact-link.white:hover { color: var(--white); background: rgba(255,255,255,0.05); border-color: rgba(255,255,255,0.2); }

    /* ─── FOOTER ─── */
    footer {
      position: relative;
      z-index: 10;
      border-top: 1px solid rgba(0,255,0,0.1);
      background: rgba(0,0,0,0.5);
      padding: 3rem 1.5rem 2rem;
    }
    .footer-inner {
      max-width: 1280px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 20px;
    }
    .footer-brand { font-family: 'Orbitron', monospace; font-size: 0.9rem; font-weight: 700; }
    .footer-brand .green { color: var(--primary); }
    .footer-brand .dim { color: var(--gray-500); }
    .footer-copy { font-size: 0.75rem; color: var(--gray-500); font-family: 'JetBrains Mono', monospace; }
    .footer-links { display: flex; gap: 20px; }
    .footer-links a { font-size: 0.78rem; color: var(--gray-500); transition: color 0.2s; font-family: 'JetBrains Mono', monospace; }
    .footer-links a:hover { color: var(--primary); }

    /* ─── DIVIDER ─── */
    .divider {
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(0,255,0,0.15), transparent);
      max-width: 1100px;
      margin: 0 auto;
    }

    /* ─── ANIMATIONS ─── */
    @keyframes glowPulse {
      0%, 100% { box-shadow: 0 0 10px rgba(0,255,0,0.15); }
      50%       { box-shadow: 0 0 24px rgba(0,255,0,0.3); }
    }
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50%       { opacity: 0.3; }
    }
    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-16px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes scrollLine {
      0%   { opacity: 0; transform: scaleY(0); transform-origin: top; }
      40%  { opacity: 1; transform: scaleY(1); transform-origin: top; }
      60%  { opacity: 1; transform: scaleY(1); transform-origin: bottom; }
      100% { opacity: 0; transform: scaleY(0); transform-origin: bottom; }
    }

    /* ─── SCROLL INDICATOR ─── */
    .scroll-indicator {
      display: flex; flex-direction: column; align-items: center; gap: 8px;
      margin-top: 3rem;
      animation: fadeInUp 0.8s ease 1s both;
    }
    .scroll-indicator span { font-size: 0.65rem; color: var(--gray-500); letter-spacing: 0.15em; text-transform: uppercase; }
    .scroll-mouse {
      width: 22px; height: 36px;
      border: 2px solid rgba(0,255,0,0.2);
      border-radius: 11px;
      display: flex; justify-content: center; padding-top: 6px;
    }
    .scroll-dot {
      width: 3px; height: 8px;
      background: var(--primary);
      border-radius: 2px;
      animation: scrollLine 2s ease-in-out infinite;
    }

    /* ─── RESPONSIVE ─── */
    @media (max-width: 900px) {
      .about-grid { grid-template-columns: 1fr; text-align: center; }
      .about-list li { justify-content: center; }
      .projects-grid { grid-template-columns: 1fr 1fr; }
      .skills-grid { grid-template-columns: repeat(3, 1fr); }
      .stats-row { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 600px) {
      .projects-grid { grid-template-columns: 1fr; }
      .skills-grid { grid-template-columns: repeat(2, 1fr); }
      .nav-links .nav-link { display: none; }
      .cta-box { padding: 36px 24px; }
      .footer-inner { flex-direction: column; text-align: center; }
      .footer-links { justify-content: center; }
    }

    /* ─── ICONS (inline SVG helpers) ─── */
    .icon { display: inline-block; vertical-align: middle; }
  </style>
</head>
<body>

  <!-- Neon grid background -->
  <div class="neon-bg"></div>

  <!-- ─── NAV ─── -->
  <nav>
    <div class="container nav-inner">
      <div class="logo">
        <div class="logo-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="#00ff00" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>
          </svg>
        </div>
        <div>
          <div class="logo-text">
            <span class="logo-wolf">WOLF</span><span class="logo-tech"> TECH</span>
          </div>
          <div class="logo-sub">I am just an explorer</div>
        </div>
      </div>
      <div class="nav-links">
        <a href="#about" class="nav-link">About</a>
        <a href="#projects" class="nav-link">Projects</a>
        <a href="#skills" class="nav-link">Skills</a>
        <a href="#contact" class="nav-cta">Contact</a>
      </div>
    </div>
  </nav>

  <!-- ─── HERO ─── -->
  <div class="hero">
    <div class="hero-badge">
      <div class="hero-badge-dot"></div>
      Available for freelance &amp; collaborations
    </div>

    <h1 class="hero-title">
      <span class="hero-title-white">BUILD.<br/>DEPLOY.</span><br/>
      <span class="hero-title-green">DOMINATE.</span>
    </h1>

    <p class="hero-tagline">// Silent Wolf — Full-Stack Developer &amp; Bot Engineer</p>

    <p class="hero-desc">
      I build production-grade systems — WhatsApp bots, web platforms,
      VPS infrastructure, and automation tools. I ship real products, fast.
    </p>

    <div class="hero-btns">
      <a href="#projects" class="btn-primary">
        <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
        View Projects
      </a>
      <a href="#contact" class="btn-ghost">
        <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
        Get In Touch
      </a>
    </div>

    <div class="stats-row">
      <div class="stat-card">
        <div class="stat-value">5+</div>
        <div class="stat-label">Projects Shipped</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">24/7</div>
        <div class="stat-label">Uptime Systems</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">3+</div>
        <div class="stat-label">Years Building</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">∞</div>
        <div class="stat-label">Curiosity</div>
      </div>
    </div>

    <div class="scroll-indicator">
      <div class="scroll-mouse"><div class="scroll-dot"></div></div>
      <span>Scroll</span>
    </div>
  </div>

  <div class="divider"></div>

  <!-- ─── ABOUT ─── -->
  <section id="about">
    <div class="container">
      <div class="about-grid">
        <div class="about-avatar-wrap">
          <div class="about-avatar">
            <svg viewBox="0 0 24 24" fill="none" stroke="rgba(0,255,0,0.5)" stroke-width="1.5">
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>
            </svg>
          </div>
        </div>
        <div class="about-content">
          <p class="section-label">// who am i</p>
          <h2 class="section-title">Silent Wolf</h2>
          <p class="section-desc">
            A self-taught developer from Kenya who loves building things that actually work.
            From WhatsApp bots to full-stack hosting platforms — I take ideas and ship them
            into production-ready products.
          </p>
          <ul class="about-list">
            <li>Full-stack web development (React, Node.js, Express)</li>
            <li>WhatsApp bot development &amp; automation</li>
            <li>VPS &amp; cloud infrastructure (Nginx, PM2, Contabo, Heroku)</li>
            <li>Database design (SQLite, PostgreSQL, Drizzle ORM)</li>
            <li>Open to freelance, collabs &amp; long-term projects</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <div class="divider"></div>

  <!-- ─── PROJECTS ─── -->
  <section id="projects">
    <div class="container" style="text-align:center">
      <p class="section-label">// what i've built</p>
      <h2 class="section-title">Projects</h2>
      <p class="section-desc" style="margin:0 auto">
        A selection of real projects I've designed, built, and deployed to production.
      </p>

      <div class="projects-grid">

        <!-- wolfXnode — featured -->
        <div class="project-card featured">
          <div class="project-tag">LIVE</div>
          <div class="project-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="#00ff00" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="3" width="20" height="14" rx="2"/><polyline points="8 21 12 17 16 21"/>
            </svg>
          </div>
          <div class="project-name">wolfXnode</div>
          <p class="project-desc">
            WhatsApp bot hosting platform. Deploy bots from GitHub in one click,
            manage via web dashboard, pay with M-Pesa or card. Running on Contabo VPS.
          </p>
          <div class="project-tech">
            <span class="tech-badge">React</span>
            <span class="tech-badge">Node.js</span>
            <span class="tech-badge">SQLite</span>
            <span class="tech-badge">PM2</span>
            <span class="tech-badge">Nginx</span>
          </div>
          <a href="https://host.xwolf.space" target="_blank" class="project-link">
            View Live
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
          </a>
        </div>

        <!-- Project 2 — placeholder -->
        <div class="project-card">
          <div class="project-tag">WIP</div>
          <div class="project-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="#00ff00" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
            </svg>
          </div>
          <div class="project-name">Project Two</div>
          <p class="project-desc">
            Short description of what this project does, the problem it solves,
            and who it's for. Replace with your real project details.
          </p>
          <div class="project-tech">
            <span class="tech-badge">Tech A</span>
            <span class="tech-badge">Tech B</span>
            <span class="tech-badge">Tech C</span>
          </div>
          <a href="#" class="project-link">
            View Project
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14"/><path d="M12 5l7 7-7 7"/></svg>
          </a>
        </div>

        <!-- Project 3 — placeholder -->
        <div class="project-card">
          <div class="project-tag">OPEN SOURCE</div>
          <div class="project-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="#00ff00" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>
            </svg>
          </div>
          <div class="project-name">Project Three</div>
          <p class="project-desc">
            Another project — automation tool, bot, CLI, or API. Describe what
            it does and why you built it. Replace with your real project.
          </p>
          <div class="project-tech">
            <span class="tech-badge">Tech A</span>
            <span class="tech-badge">Tech B</span>
          </div>
          <a href="#" class="project-link">
            GitHub
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
          </a>
        </div>

      </div>
    </div>
  </section>

  <div class="divider"></div>

  <!-- ─── SKILLS ─── -->
  <section id="skills">
    <div class="container" style="text-align:center">
      <p class="section-label">// what i use</p>
      <h2 class="section-title">Tech Stack</h2>
      <p class="section-desc" style="margin:0 auto">Tools and technologies I work with day to day.</p>

      <div class="skills-grid">
        <div class="skill-card"><div class="skill-icon">⚛️</div><div class="skill-name">React</div><div class="skill-level">Frontend</div></div>
        <div class="skill-card"><div class="skill-icon">🟩</div><div class="skill-name">Node.js</div><div class="skill-level">Backend</div></div>
        <div class="skill-card"><div class="skill-icon">🗃️</div><div class="skill-name">SQLite</div><div class="skill-level">Database</div></div>
        <div class="skill-card"><div class="skill-icon">🐘</div><div class="skill-name">PostgreSQL</div><div class="skill-level">Database</div></div>
        <div class="skill-card"><div class="skill-icon">🔧</div><div class="skill-name">Express</div><div class="skill-level">API</div></div>
        <div class="skill-card"><div class="skill-icon">🌐</div><div class="skill-name">Nginx</div><div class="skill-level">Server</div></div>
        <div class="skill-card"><div class="skill-icon">♻️</div><div class="skill-name">PM2</div><div class="skill-level">Process Mgmt</div></div>
        <div class="skill-card"><div class="skill-icon">🐙</div><div class="skill-name">GitHub</div><div class="skill-level">Version Control</div></div>
        <div class="skill-card"><div class="skill-icon">🎨</div><div class="skill-name">Tailwind CSS</div><div class="skill-level">Styling</div></div>
        <div class="skill-card"><div class="skill-icon">🤖</div><div class="skill-name">WhatsApp Bots</div><div class="skill-level">Automation</div></div>
        <div class="skill-card"><div class="skill-icon">📦</div><div class="skill-name">VPS / Linux</div><div class="skill-level">Infrastructure</div></div>
        <div class="skill-card"><div class="skill-icon">⚡</div><div class="skill-name">Vite</div><div class="skill-level">Build Tool</div></div>
      </div>
    </div>
  </section>

  <div class="divider"></div>

  <!-- ─── CTA / CONTACT ─── -->
  <section id="contact">
    <div class="container">
      <div class="cta-box">
        <p class="section-label" style="margin-bottom:0.5rem">// let's build</p>
        <h2 class="cta-title">Have a project in mind?</h2>
        <p class="cta-desc">
          Whether it's a bot, a web platform, or a full production deployment —
          let's talk. I work fast and I ship real things.
        </p>
        <div class="contact-links">
          <a href="https://wa.me/254713046497" target="_blank" class="contact-link">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
            WhatsApp
          </a>
          <a href="https://www.youtube.com/@Silentwolf906" target="_blank" class="contact-link">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor"><path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/></svg>
            YouTube
          </a>
          <a href="https://github.com/sil3nt-wolf" target="_blank" class="contact-link white">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
            GitHub
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ─── FOOTER ─── -->
  <footer>
    <div class="footer-inner">
      <div>
        <div class="footer-brand">
          <span class="green">WOLF</span><span class="dim"> TECH</span>
        </div>
        <div class="footer-copy" style="margin-top:6px">
          &copy; 2026 Silent Wolf. All rights reserved.
        </div>
      </div>
      <div class="footer-links">
        <a href="#about">About</a>
        <a href="#projects">Projects</a>
        <a href="#skills">Skills</a>
        <a href="#contact">Contact</a>
        <a href="https://host.xwolf.space" target="_blank">wolfXnode</a>
      </div>
    </div>
  </footer>

  <script>
    // Smooth scroll for nav links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', e => {
        const target = document.querySelector(anchor.getAttribute('href'));
        if (target) {
          e.preventDefault();
          target.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      });
    });

    // Intersection observer — fade-in on scroll
    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.project-card, .skill-card, .stat-card').forEach(el => {
      el.style.opacity = '0';
      el.style.transform = 'translateY(20px)';
      el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
      observer.observe(el);
    });
  </script>
</body>
</html>
